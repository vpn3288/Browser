# v14.1 修复说明 - 基于v13.7的关键修复

## 🎯 修复策略

**基础版本：** v13.7（已有完整浏览器选择菜单和大部分正确配置）  
**修复方式：** 只修复关键问题，不做过度优化

## ✅ 必须修复的问题（共7个）

### 1. 允许登录和同步（最重要）
```powershell
# v13.7 的错误配置
SigninAllowed = 0  # ❌ 禁止登录
BrowserSignin = 0  # ❌ 禁止浏览器登录
SyncDisabled = 1   # ❌ 禁用同步

# v14.1 修复
SigninAllowed = 1  # ✅ 允许登录
BrowserSignin = 1  # ✅ 允许浏览器登录
(删除 SyncDisabled)  # ✅ 允许同步
```

### 2. 允许导入书签
```powershell
# v13.7 的错误配置
ImportBookmarks = 0  # ❌ 禁止导入

# v14.1 修复
ImportBookmarks = 1  # ✅ 允许导入
ImportHistory = 1
ImportSavedPasswords = 1
ImportAutofillFormData = 1
```

### 3. 启用安全浏览（修复CF验证）
```powershell
# v13.7 的错误配置
SafeBrowsingEnabled = 0  # ❌ 导致CF验证失败

# v14.1 修复
SafeBrowsingEnabled = 1  # ✅ 启用安全浏览
SSLErrorOverrideAllowed = 1
```

### 4. 修复WebRTC策略名称
```powershell
# v13.7 可能的错误
WebRtcIPHandlingPolicy = "disable_non_proxied_udp"  # ⚠️ 策略名可能不对

# v14.1 修复（按浏览器区分）
# Chrome/Chromium/Brave/Opera/Vivaldi:
WebRtcIPHandlingPolicy = "disable_non_proxied_udp"

# Edge:
WebRtcLocalhostIpHandling = "disable_non_proxied_udp"
```

### 5. 修复新标签页/主页设置
```powershell
# v13.7 的错误配置
RestoreOnStartup = 1  # ❌ 恢复上次会话

# v14.1 修复
RestoreOnStartup = 5  # ✅ 打开新标签页
NewTabPageLocation = "about:blank"
HomepageIsNewTabPage = 1
HomepageLocation = "about:blank"
```

### 6. Firefox DisableFirefoxAccounts
```json
// v13.7 的错误配置
"DisableFirefoxAccounts": true  // ❌ 禁用账号

// v14.1 修复
// (删除此项)  // ✅ 允许登录
```

### 7. Firefox user.js 书签新标签页打开
```javascript
// v13.7 缺失

// v14.1 添加
user_pref("browser.tabs.loadBookmarksInTabs", true);
user_pref("browser.tabs.loadBookmarksInBackground", false);
```

## ⚠️ 保留v13.7的有用配置

### 保留：关闭后台运行
```powershell
BackgroundModeEnabled = 0  # ✅ 保留
```

### 保留：禁用默认浏览器弹窗
```powershell
DefaultBrowserSettingEnabled = 0  # ✅ 保留

# Edge 额外添加
DefaultBrowserSettingsCampaignEnabled = 0
StartupBoostEnabled = 0
```

### 保留：厂商私货屏蔽
```powershell
# Edge
EdgeShoppingAssistantEnabled = 0
EdgeCollectionsEnabled = 0
ShowRecommendationsEnabled = 0
HubsSidebarEnabled = 0
EdgeWorkspacesEnabled = 0
EdgeWalletEnabled = 0

# Brave
BraveRewardsDisabled = 1
BraveWalletDisabled = 1
BraveAdsEnabled = 0
BraveVPNEnabled = 0
IPFSEnabled = 0

# Chrome/Chromium
MediaRouterEnabled = 0
PromotionalTabsEnabled = 0
```

## ❌ 删除v13.7的过度优化

### 删除：User-Agent硬编码
```powershell
# v13.7 有硬编码的旧版本UA
$userAgentConfig = @{...}  # ❌ 删除整个配置

# v14.1：不设置UA，让浏览器使用默认
# 原因：硬编码旧版本UA比不伪装更假
```

### 删除：UserAgentClientHintsEnabled
```powershell
# v13.7
UserAgentClientHintsEnabled = 0  # ❌ 删除

# v14.1：不设置此项
# 原因：缺失Client Hints本身就是异常特征
```

### 删除：DNS-over-HTTPS强制锁定
```powershell
# v13.7
DnsOverHttpsMode = "secure"  # ❌ 过于强制

# v14.1
DnsOverHttpsMode = "automatic"  # ✅ 自动模式
# 或者完全不设置，让用户自己配置
```

### 删除：Firefox resistFingerprinting
```javascript
// v13.7
user_pref("privacy.resistFingerprinting", true);  // ❌ 过于激进

// v14.1：改为可选（注释掉）
// user_pref("privacy.resistFingerprinting", true);  // 可选：强隐私模式
user_pref("privacy.trackingprotection.fingerprinting.enabled", true);  // ✅ 温和的指纹保护
```

## 📝 实施步骤

1. 复制 v13.7 为 v14.1
2. 修改版本号和说明
3. 应用上述7个必须修复
4. 保留有用配置
5. 删除过度优化
6. 测试验证

## 🔍 验证清单

- [ ] chrome://policy 显示 SigninAllowed = 1
- [ ] chrome://policy 显示 ImportBookmarks = 1
- [ ] chrome://policy 显示 SafeBrowsingEnabled = 1
- [ ] chrome://policy 显示 WebRTC策略有效
- [ ] 浏览器可以登录账号
- [ ] Chromium可以导入书签
- [ ] CF验证正常通过
- [ ] 甲骨文云正常访问
- [ ] Firefox可以登录账号
- [ ] 书签在新标签页打开

## 📋 封笔标准

修复完成后，如果：
1. ✅ 所有浏览器可以登录
2. ✅ 可以导入书签
3. ✅ CF验证正常
4. ✅ 核心反检测保留（WebRTC、遥测、追踪）
5. ✅ 没有虚假优化
6. ✅ 没有负优化

则封笔，不再修改。
